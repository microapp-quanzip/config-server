server:
  port: 9999

eureka:
  client:
    register-with-eureka: true
    #     it is a discovery server already, so no need to fetch status of it-self,
    #  instead, we should fetch status of services that register to this discovery
    fetch-registry: false


